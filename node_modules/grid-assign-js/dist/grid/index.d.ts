import { point } from "../lap-jv/";
interface IGenerateGrid {
    width: number;
    height: number;
    numRows: number;
    numColumns: number;
    translate?: point;
    centerPoint?: boolean;
}
export declare function generateGrid({ width, height, numRows, numColumns, centerPoint, translate, }: IGenerateGrid): {
    gridHeight: number;
    gridWidth: number;
    gridPoints: point[];
};
declare type ScaleLinear = (x: number) => number;
interface IGridAssignment {
    points: point[];
    screenWidth: number;
    screenHeight: number;
    screenX?: number;
    screenY?: number;
    numColumns: number;
    numRows: number;
    anchorMiddleGrid?: boolean;
    returnPointConversion?: boolean;
}
export declare function autoGridAssignment({ points, screenWidth, screenHeight, screenX, screenY, numColumns, numRows, anchorMiddleGrid, }: IGridAssignment): {
    assignments: {
        gridPoint: point;
        assignedPoint: point;
        assignedPointIndex: number;
        gridWidth: number;
        gridHeight: number;
    }[];
    pointConversions: {
        pointToScreen: {
            x: ScaleLinear;
            y: ScaleLinear;
        };
        screenToPoint: {
            x: ScaleLinear;
            y: ScaleLinear;
        };
        apply2D: (point: point, converter: {
            x: ScaleLinear;
            y: ScaleLinear;
        }) => number[];
    };
};
export {};
